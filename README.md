# NodeJS Brukerregistrering og innlogging
Se https://docs.iktim.no for steg-for-steg. Dette er ferdig kildekode.

Lastes ned i terminalen via `npm install`, kjøres via `npm run watch` eller `npm run test`
